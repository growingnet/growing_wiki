# Graphviz Sources

Put `.dot` files in this directory.

Build contract:

- Input: `docs/_figures_src/dot/<name>.dot`
- Output: `docs/_static/<name>.svg`
- Command: `scripts/build_figures.sh`

Use this for process graphs and topology diagrams where graph structure is primary.
