repos:
  - repo: local
    hooks:
      - id: docs-strict-build
        name: docs strict build
        entry: sphinx-build -W --keep-going -E -a -b html docs docs/_build/html
        language: system
        pass_filenames: false
        always_run: true
