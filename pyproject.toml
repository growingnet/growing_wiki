[project]
name = "growing-wiki"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "sphinx>=7.2,<8",
    "sphinx-autobuild",
    "furo",
    "sphinxcontrib-bibtex",
    "pytest",
    "pre-commit",
]
